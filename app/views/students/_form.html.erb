<%= form_with(model: student, local: true) do |form| %>
  <% if student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
      <% student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :username %>
    <%= form.text_field :username, id: :student_username %>
  </div>

  <div class="field">
    <%= form.label :birth %>
    <%= form.date_select :birth, id: :student_birth %>
  </div>

  <div class="field">
    <%= form.label :graduate %>
    <%= form.number_field :graduate, id: :student_graduate %>
  </div>

  <div class="field">
    <%= form.label :experience %>
    <%= form.text_area :experience, id: :student_experience %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
